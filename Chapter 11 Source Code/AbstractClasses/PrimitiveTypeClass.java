public abstract class PrimitiveTypeClass
{
   public abstract boolean equals(PrimitiveTypeClass right);
      //Method to return true if two objects contain the same
      //data

   public abstract int compareTo(PrimitiveTypeClass right);
      //Method to compare two objects

   public abstract String toString(); 
       //Method to return data as a string
}
